# bw-009: Debug eprintf left in shotgun_surgery.ml production code

**Severity:** High  
**Status:** Fixed  
**File:** `lib/catseye_claws/shotgun_surgery.ml:111`

## Problem

```ocaml
Stdlib.Printf.eprintf "[Shotgun] Total entries in table: %d\n" (Stdlib.Hashtbl.length receiver_calls);
```

Writes to stderr on every analysis run. I/O side effect in what should be a pure analysis function.

## Fix

Remove the line.
