# bw-029: Misc small DRY + dead code + no-op issues

**Severity:** Low  
**Status:** Fixed  
**Files:** Multiple

## Problems

1. **db.ml:37,99** — `is_tainted` and `is_tainted_anywhere` are character-for-character identical. Make alias.

2. **crystal_mapper.ml:159-174** — `find_scope_end` is dead code with O(n²) behavior. Remove.

3. **cfg_dominator.ml:66-82** — `block_has_rule_sanitizer` has O(n²) loop with unused `_pat` variable. Actually dead code. Remove.

4. **of_catseye_ast.ml:172** — Pointless `let _ = () in` binding. Remove.

5. **rust_mapper.ml:24-37** — Unnecessary XML type conversion (deep copy of identical type). Use structural sharing like other mappers.

6. **heatmap.ml:53** — No-op early return: `if findings = [] then ()` doesn't return from function.

7. **orchestrator.ml:911-912** — Duplicate `let sup = config.taint_suppress in` binding.

8. **pipeline.ml:89** — `find_dependency_cycles` always returns `[]` (unfinished). Remove or implement.

9. **analysis_graph.ml:24-32** — `invalidate_file` is a no-op (assigns `g.files <- g.files`).

10. **state_manager.ml:160** — Debug `eprintf` in production cache lookup.
