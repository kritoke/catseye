# bw-026: DRY — Consolidate claws shared utilities

**Severity:** Medium (DRY)  
**Status:** Open  
**Files:** Multiple claws files

## Problem

- `find_substring` defined 3× (scope.ml, anatomy.ml, complexity.ml)
- `contains` defined 8× across detectors
- `build_scopes` defined 3× with divergent logic (scope.ml, anatomy.ml, concurrency.ml — concurrency's is buggy, see bw-017)
- `is_benchmark_or_example` defined 4× with different suffix lists
- `is_exempt_method` duplicated in anatomy.ml / anatomy_ast.ml
- `is_dry_exempt_file` duplicated in dry.ml / dry_ast.ml
- Finding record construction boilerplate duplicated 20+ times

## Fix

Consolidate into `scope.ml` or a new `smell_helpers.ml`. Have detectors import from shared module.
